/**
 * Production settings. Replace apiUrl with your Render service URL after the
 * first deploy — it looks like https://portfolio-api-xxxx.onrender.com/api
 */
export const environment = {
  production: true,
  apiUrl: 'https://your-service-name.onrender.com/api',
} as const;
